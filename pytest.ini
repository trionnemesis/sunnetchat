[tool:pytest]
markers =
    asyncio: mark test as an asyncio test
addopts = 
    -v
    --strict-markers
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*