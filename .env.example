# General Settings
# LOCAL_KNOWLEDGE_BASE_PATH: Path to your local files (e.g., C:\SUNNET or /mnt/c/SUNNET on WSL)
LOCAL_KNOWLEDGE_BASE_PATH="/mnt/c/SUNNET"

# Ollama Configuration
# OLLAMA_BASE_URL: URL for the Ollama service. 'http://host.docker.internal:11434' is used to allow the Docker container to access Ollama running on the host machine.
OLLAMA_BASE_URL="http://host.docker.internal:11434"
OLLAMA_MODEL="llama3"

# Embedding Model Configuration
# EMBEDDING_MODEL: The name of the Google embedding model to use.
EMBEDDING_MODEL="models/embedding-001"

# Vector Database Configuration
# VECTOR_DB_PATH: The local directory to store the Chroma vector database.
VECTOR_DB_PATH="chroma_db"

# --- Phase 2 Credentials ---
# Slack API Credentials
SLACK_BOT_TOKEN="xoxb-your-bot-token-here"
SLACK_SIGNING_SECRET="your-slack-signing-secret-here"

# --- Phase 3 Credentials ---
# Google Drive API Credentials
# GOOGLE_DRIVE_FOLDER_ID: The ID of the Google Drive folder where new knowledge will be stored.
GOOGLE_DRIVE_FOLDER_ID="your-google-drive-folder-id-here"
# GOOGLE_APPLICATION_CREDENTIALS_JSON: The content of your Google service account JSON file.
# It's recommended to paste the entire JSON content here, enclosed in single quotes.
GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type": "service_account", "project_id": "...", ...}'

# Tavily API for Web Search
TAVILY_API_KEY="tvly-your-tavily-api-key-here"
